{
  "name": "cypress-tests",
  "version": "1.0.0",
  "description": "Automated Cypress tests for Parabank application",
  "scripts": {
    "test": "cypress run --reporter mocha-multi-reporters --reporter-options configFile=cypress-reporter.json"
  },
  "devDependencies": {
    "cypress": "^12.17.1",
    "mocha": "^10.2.0",
    "mocha-multi-reporters": "^1.5.1",
    "mochawesome": "^7.1.3"
  }
}
